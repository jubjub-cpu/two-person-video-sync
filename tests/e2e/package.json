{
  "name": "@vyzync/e2e",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:extension": "pnpm --filter @vyzync/protocol build && pnpm --filter @vyzync/sync-core build && pnpm --filter @vyzync/extension build:chrome:test && node prepare-extension.mjs",
    "test": "pnpm prepare:extension && pnpm --filter @vyzync/server build && playwright test",
    "test:public-smoke": "pnpm --filter @vyzync/extension build:chrome && node public-smoke.mjs",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@playwright/test": "1.62.0",
    "@types/node": "^24.1.0",
    "typescript": "^5.8.3"
  }
}
